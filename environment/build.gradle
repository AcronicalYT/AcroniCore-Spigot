plugins {
    id 'java-library'
}

dependencies {
    api project(':environment:item-core')
//    api project(':environment:region-core')
    // api project(':environment:world-core')
    // api project(':environment:teleport-core')
}